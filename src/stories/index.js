
import './../main.css';
import 'normalize.css';
import './../ui/components/auth/stories';
import './../ui/components/diary/stories';
import './../ui/components/generic/stories';


/*
import React from 'react';
import { storiesOf, action, linkTo } from '@kadira/storybook';
import Button from './Button';
import Welcome from './Welcome';
import { HashesList } from './../ui/components/diary/_hashes-list';

storiesOf('Welcome', module)
  .add('to Storybook', () => (
    <Welcome showApp={linkTo('Button')}/>
  ));

storiesOf('Button', module)
  .add('with text', () => (
    <Button onClick={action('clicked')}>Hello Button</Button>
  ))
  .add('with some emoji', () => (
    <Button onClick={action('clicked')}>😀 😎 👍 💯</Button>
  ));

const hashes = [
  'hallo', 'noo'
];

storiesOf('HashList', module)
  .add('empty', () => (
      <HashesList />
  ))
  .add('withContent', () => (
      <HashesList hashes={hashes} />
  ));
    */